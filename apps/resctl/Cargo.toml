[package]
name = "resctl"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Resonance CLI — every capability, scriptable"

[dependencies]
resonance-core.workspace = true
tuner.workspace = true
